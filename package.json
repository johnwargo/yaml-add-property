{
  "name": "yaml-add-property",
  "version": "0.0.1",
  "description": "Adds the specified YAML property to all files in the specified directory.",
  "author": "John M. Wargo",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/johnwargo/yaml-add-property.git"
  },
  "type": "module",
  "main": "yaml-add-property.js",
  "bin": {
    "yaml-add-prop": "yaml-add-property.js"
  },
  "keywords": [
    "node"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "tsc && node yaml-add-property.js -d",
    "start": "tsc && node yaml-add-property.js"
  },
  "dependencies": {
    "cli-logger": "^0.5.40",
    "commander": "^10.0.0",
    "fs-extra": "^11.1.1",
    "yaml": "^2.2.1"
  },
  "devDependencies": {
    "@types/fs-extra": "^11.0.1",
    "@types/node": "^18.15.5",
    "eleventy": "^1.0.6"
  }
}
